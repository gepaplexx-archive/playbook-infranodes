---
- name: infranodes
  hosts:
    - all
  become: true
  tags:
    - kubernetes
    - openshift
    - openshift.infranodes
  roles:
    - {role: gepaplexx.infranodes, when: ocp_infranodes_count > 0}
...
